# ChatBot-App
An interactive AI ChatBot built using Flask and HuggingFace DialoGPT, designed as a simple conversational web application. The project runs on a local Flask server and generates responses in real time using a pre-trained NLP model.


